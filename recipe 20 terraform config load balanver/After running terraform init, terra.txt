After running terraform init, terraform plan, and terraform apply, first go to the top-right corner of AWS Console and make sure the region is set to Mumbai (ap-south-1) because Terraform deployed all resources there. Then go to EC2 → Instances and show the two running instances named Server1 and Server2. After that, go to Load Balancers and open the Application Load Balancer named my-alb. Copy the DNS name and open it in the browser. Refresh the page multiple times and show that requests are being distributed between “Server 1” and “Server 2”. This proves the Load Balancer is working correctly and distributing traffic across multiple EC2 instances.

Next, go to Target Groups → my-target-group and show that both EC2 instances are registered and healthy. Then go to VPC, Subnets, Internet Gateway, and Route Tables to show the networking resources created automatically by Terraform. Also show the security group allowing HTTP (80) and SSH (22) access. Finally, show the Terraform terminal output: