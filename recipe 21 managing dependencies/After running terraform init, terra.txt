After running terraform init, terraform plan, and terraform apply, first go to the top-right corner of AWS Console and make sure the region is set to Mumbai (ap-south-1) because Terraform deployed all resources there. Then go to VPC → Your VPCs and show the VPC named MyVPC. After that, go to Subnets and show the subnet MySubnet. Then open Internet Gateways and show the attached Internet Gateway. Next, go to Route Tables and show the route 0.0.0.0/0 connected to the Internet Gateway. This proves Terraform successfully managed infrastructure dependencies automatically in the correct order.

Next, go to EC2 → Security Groups and show the security group allowing SSH on port 22. Then go to EC2 → Instances and show the EC2 instance named MyEC2 running successfully. Explain to the professor that Terraform first created the VPC, then subnet, internet gateway, route table, route table association, security group, and finally the EC2 instance automatically based on dependencies. Finally, show the Terraform terminal output: